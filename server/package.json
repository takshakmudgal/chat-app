{
  "name": "chat-app-server",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "dev": "bun --hot server.ts",
    "serve": "bun --hot server.ts",
    "build": "bun build ./server.ts --outdir=dist --target=node --minify",
    "start": "NODE_ENV=production bun dist/server.js",
    "prod": "NODE_ENV=production bun server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "bun@1.2.18",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "dotenv": "^17.1.0"
  }
}
